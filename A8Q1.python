import tkinter as tk
from tkinter import messagebox

window = tk.Tk()
window.title("Alert Example")

def show_alert():
    messagebox.showinfo("Alert", "Button was pressed!")

alert_button = tk.Button(window, text="Press Me", command=show_alert)
alert_button.pack(pady=20)

window.mainloop()
